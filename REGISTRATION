CREATE DATABASE Registration;
USE Registration;
CREATE TABLE Page(
  User_Name VARCHAR(255),
  Password VARCHAR(255),
  Con-Password VARCHAR(255),
  Email VARCHAR(255)
);
ALTER TABLE Page
ADD PRIMARY KEY (User_Name);
DESCRIBE Page;
